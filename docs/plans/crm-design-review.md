# Ревью дизайна обзора CRM

Дата: 2026-09-19.
Область: обзор `/crm`, общие статусы, навигация и контраст тем.
Модель карточки, API, сохранение данных и поведение правой панели не изменялись.

## Находки и исправления

- `apps/web/src/components/crm/dashboard.tsx:47`: две широкие таблицы обрезали колонки.
  Связанные поля сгруппированы, две колонки включаются от 1600 px, ниже блоки идут последовательно.
- `apps/web/src/components/crm/buyer-table.tsx:19`: семь колонок не помещались в миниатюре.
  ДР находится под именем, оплата под бюджетом, статус под этапом; температура остается отдельным полем.
- `apps/web/src/components/crm/seller-table.tsx:26`: адрес обрезался, крайние поля уходили за границу блока.
  Адрес переносится, цена и объект сгруппированы, ДР расположен под именем.
- `apps/web/src/components/crm/dashboard.tsx:22`: ограниченная высота задач обрезала последнюю строку.
  Обзор показывает первые пять задач, число показанных записей и переход к полному списку.
- `apps/web/src/components/crm/task-table.tsx:29`: заголовки 11 px и основной текст 13 px были слишком мелкими.
  Теперь заголовки 12 px, основной текст 14 px, высота строки заголовка 44 px.
- `apps/web/src/components/crm/inline-select.tsx:32`: маленькие контролы без видимого клавиатурного фокуса.
  Высота увеличена до 32 px, текст до 12 px, добавлен focus-visible и явные цвета пунктов списка.
- `apps/web/src/components/crm/sidebar.tsx:86`: тесные пункты навигации и мелкие подписи.
  Ширина панели 240 px, пункты высотой 44 px, текст 14 px.
- `apps/web/src/app/globals.css:140`: слабый контраст вторичного текста в светлой теме.
  Контраст вторичного текста на карточке теперь 4.97:1 в светлой теме и 6.68:1 в темной.
  Проверенные голубой, синий, желтый и зеленый статусы в светлой теме имеют контраст выше 4.5:1.

Ревью учитывает [Web Interface Guidelines](https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md).

## Проверка

- `pnpm --filter web lint`: успешно.
- `pnpm --filter web build`: успешно, включая TypeScript.
- `git diff --check`: успешно.
- Ширины 1024, 1440, 1600 и 1728 px: таблицы обзора помещаются без горизонтального переполнения.
- Ширина 390 px: страница не переполняется, таблицы прокручиваются горизонтально, есть текстовая подсказка.
- Фильтр «Сегодня»: три задачи; «Показы»: корректное пустое состояние.
- Полный список задач содержит все 22 задачи тестового набора.
- Открытие карточки, переключение на другого клиента при открытой панели, fullscreen и возврат в обзор проверены.
- Console: ошибок и предупреждений нет; проверенные запросы навигации вернули HTTP 200.
- Данные клиентов в этой проверке не изменялись; повторная проверка записи в API не проводилась.

## Визуальные артефакты

- `output/playwright/overview-before.png`
- `output/playwright/overview-dark-wide.png`
- `output/playwright/overview-light-wide.png`
- `output/playwright/overview-dark-1024.png`
- `output/playwright/overview-dark-1440.png`
- `output/playwright/overview-dark-1600.png`
- `output/playwright/overview-dark-mobile.png`

Скриншоты локальные и исключены из git.
Деплой и коммит не выполнялись.
Запущенные ранее dev-серверы оставлены работающими.
